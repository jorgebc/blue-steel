<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  <changeSet id="0005-create-refresh-tokens" author="jorge">

    <createTable tableName="refresh_tokens">
      <column name="id" type="UUID">
        <constraints primaryKey="true" nullable="false"/>
      </column>
      <column name="user_id" type="UUID">
        <constraints nullable="false"
                     foreignKeyName="fk_refresh_tokens_user"
                     references="users(id)"/>
      </column>
      <!-- D-059: SHA-256 of the raw token; raw token is never stored -->
      <column name="token_hash" type="TEXT">
        <constraints nullable="false"/>
      </column>
      <!-- D-059: groups a login session's rotation chain for family-based reuse detection -->
      <column name="family_id" type="UUID">
        <constraints nullable="false"/>
      </column>
      <column name="expires_at" type="TIMESTAMP WITH TIME ZONE">
        <constraints nullable="false"/>
      </column>
      <!-- D-059: nullable; set when this token is exchanged for a new one -->
      <column name="used_at" type="TIMESTAMP WITH TIME ZONE">
        <constraints nullable="true"/>
      </column>
      <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
        <constraints nullable="false"/>
      </column>
    </createTable>

    <createIndex tableName="refresh_tokens" indexName="idx_refresh_tokens_family_id">
      <column name="family_id"/>
    </createIndex>

  </changeSet>

</databaseChangeLog>
