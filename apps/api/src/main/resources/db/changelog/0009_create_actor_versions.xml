<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  <!-- Actor version table — append-only history (D-035, D-001, D-003) -->
  <changeSet id="0009-create-actor-versions" author="jorge">

    <createTable tableName="actor_versions">
      <column name="id" type="UUID">
        <constraints primaryKey="true" nullable="false"/>
      </column>
      <column name="actor_id" type="UUID">
        <constraints nullable="false"
                     foreignKeyName="fk_actor_versions_actor"
                     references="actors(id)"/>
      </column>
      <column name="session_id" type="UUID">
        <constraints nullable="false"
                     foreignKeyName="fk_actor_versions_session"
                     references="sessions(id)"/>
      </column>
      <column name="version_number" type="INTEGER">
        <constraints nullable="false"/>
      </column>
      <!-- Only the fields that changed in this version -->
      <column name="changed_fields" type="JSONB">
        <constraints nullable="true"/>
      </column>
      <!-- Complete state snapshot as of this version -->
      <column name="full_snapshot" type="JSONB">
        <constraints nullable="false"/>
      </column>
      <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
        <constraints nullable="false"/>
      </column>
    </createTable>

    <!-- Fast lookup of all versions for a given actor -->
    <sql>
      CREATE INDEX idx_actor_versions_actor_id ON actor_versions (actor_id);
    </sql>

    <rollback>
      <dropIndex tableName="actor_versions" indexName="idx_actor_versions_actor_id"/>
      <dropTable tableName="actor_versions"/>
    </rollback>

  </changeSet>

</databaseChangeLog>
