# Allow ArchUnit rules to pass when no classes match the filter (vacuous truth).
# This is correct behaviour during early phases when domain/application packages are still empty.
# All four rules in ArchitectureTest are designed to enforce boundaries once code exists — they
# should not fail simply because the target package has not been created yet (D-037).
archRule.failOnEmptyShould=false
