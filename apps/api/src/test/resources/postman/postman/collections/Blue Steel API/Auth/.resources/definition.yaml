$kind: collection
description: |-
  AuthController — /api/v1/auth
  Stateless JWT authentication. Login issues an access token + httpOnly refresh token cookie.
  Refresh rotates the token pair. Logout revokes the family and clears the cookie (D-059).
order: 1000
