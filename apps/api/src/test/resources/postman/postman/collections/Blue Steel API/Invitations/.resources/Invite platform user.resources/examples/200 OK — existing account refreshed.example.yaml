$kind: http-example
request:
  url: "{{baseUrl}}/api/v1/invitations"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: text
    content: |-
      {
        "email": "existing@example.com"
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "data": {
          "email": "existing@example.com",
          "status": "refreshed"
        },
        "meta": {},
        "errors": []
      }
order: 2000
