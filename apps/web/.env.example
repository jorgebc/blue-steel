# Copy to .env.local (gitignored) before running npm run dev.

# Backend base URL — required for all API calls.
# Local dev:    http://localhost:8080  (backend running via mvn spring-boot:run)
# Production:   set in the Vercel dashboard, not here.
VITE_API_BASE_URL=http://localhost:8080
