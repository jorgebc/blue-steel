# CLAUDE.md — Blue Steel (Repo Root)

> Entry point for all agents. Read this fully, then continue to `apps/api/CLAUDE.md` (backend) or `apps/web/CLAUDE.md` (frontend) for layer-specific context. Check `skills/SKILLS_INDEX.md` before starting any non-trivial task.

---

## 1. Project

Blue Steel is an AI-assisted narrative memory system for tabletop RPG campaigns. GMs paste raw session summaries → LLM pipeline extracts structured knowledge (actors, events, locations, relationships) → versioned world state. Users query in natural language or browse through four visual exploration views. Backend + frontend communicate exclusively over a versioned REST API (`/api/v1/`).

---

## 2. Monorepo Layout

```
blue-steel/
├── apps/
│   ├── api/               ← Java 25 / Spring Boot 4.0.3 (Maven)
│   └── web/               ← React 18 / Vite / TypeScript
├── docs/                  ← PRD, ARCHITECTURE, DECISIONS, ROADMAP (read-only)
├── docker-compose.yml     ← PostgreSQL + pgvector (local infra only)
├── .github/workflows/     ← backend.yml + frontend.yml (path-filtered)
├── skills/                ← Agent skill files — see SKILLS_INDEX.md
└── CLAUDE.md              ← This file
```

- `apps/api` and `apps/web` share no code. Their contract is the HTTP API.
- `apps/web/src/components/ui/` — auto-generated by shadcn/ui. **Never edit manually.**
- `apps/api/src/main/resources/db/changelog/` — Liquibase migrations. **Append-only.**

---

## 3. Quick Start

```bash
# Start local infra (run from repo root)
docker compose up -d

# Backend (from apps/api/)
mvn spring-boot:run -Dspring-boot.run.profiles=local   # mocked LLMs, zero API cost
mvn test              # unit + ArchUnit (fast)
mvn verify            # + integration tests (Docker required)

# Frontend (from apps/web/)
npm install && npm run dev
npx vitest run        # tests (CI mode)
```

**Required env vars** — copy `.env.example` → `.env.local`:

| Var | Purpose |
|---|---|
| `DATABASE_URL` | Neon PostgreSQL connection string |
| `ANTHROPIC_API_KEY` | Claude (text gen) — `llm-real` profile only |
| `OPENAI_API_KEY` | `text-embedding-3-small` — `llm-real` profile only |
| `JWT_SECRET` | HS256 symmetric secret (min 32 bytes) |
| `EMAIL_API_KEY` | Transactional email (Resend) |
| `VITE_API_BASE_URL` | Backend URL for frontend (e.g. `http://localhost:8080`) |

Secrets are **never committed**. `.env` and `.env.local` are always gitignored (D-050).

---

## 4. Cross-Cutting Architecture

**Auth handoff:** Backend issues JWT (HS256, 15-min TTL) + rotating refresh token (30-day, `httpOnly` cookie). JWT carries only `user_id` and `is_admin`. Campaign-level role (`gm`, `editor`, `player`) is **always resolved from `campaign_members` via DB** — never from the JWT.

**API contract:** All requests/responses are JSON. All IDs are UUIDs. All timestamps are ISO 8601 UTC. Response envelope: `{ "data": {}, "meta": {}, "errors": [] }`.

**Session ingestion flow (cross-cutting):**
1. `POST /api/v1/campaigns/{id}/sessions` → backend runs async pipeline → session status `processing`
2. Frontend polls `GET .../sessions/{sid}/status` until `draft` or `failed`
3. `GET .../sessions/{sid}/diff` → user reviews structured diff
4. `POST .../sessions/{sid}/commit` → world state written; `200` returned immediately; embeddings generated async

**UNCERTAIN cards block commit.** Backend enforces `422 UNCERTAIN_ENTITIES_PRESENT` as defence in depth. The frontend must disable the commit button first.

**One active draft per campaign** (D-054). `POST /sessions` returns `409` if another is `processing` or `draft`.

---

## 5. Cross-Cutting Conventions

**Commit format** — Conventional Commits: `type(scope): description`
- Types: `feat` `fix` `refactor` `test` `chore` `docs` `ci`
- Scopes: `api` `web` `db` `ci` `docs`

**Branch naming:** `type/short-description` kebab-case

**API error shape:** `{ "errors": [{ "code": "MACHINE_READABLE", "message": "...", "field": null }] }`

**HTTP status conventions:** 400 validation, 401 unauth, 403 unauthorized, 404 not found, 409 conflict, 422 business rule, 500 internal

**Secrets:** never committed in any form, including test fixtures (D-050).

---

## 6. Skills

Read `skills/SKILLS_INDEX.md` before starting any non-trivial task.

**Quick lookup:**
- New API endpoint → `backend-endpoint` + `backend-testing` + `database-migration`
- Session ingestion / commit → `session-ingestion-pipeline`
- Query Mode backend → `query-pipeline`
- Auth (either side) → `auth`
- Frontend data fetching → `frontend-api-resource`
- Diff review UI → `frontend-diff-review`
- Exploration Mode → `frontend-exploration`
- Schema change → `database-migration`
- Frontend test → `frontend-testing`
- CI/CD → `ci-cd`

---

## 7. Out of Scope (v1)

**Never implement:** proposal approval UI/logic, `add` action in commit payload (D-053), Q&A log (D-058), SSE streaming for queries (D-052), Spring AI `VectorStore` (D-062), E2E tests (D-056), staging environment (D-044), self-registration (D-051), real-time collaboration, audio/image ingestion, mobile app.
